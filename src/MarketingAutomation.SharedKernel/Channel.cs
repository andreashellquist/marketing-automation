namespace MarketingAutomation.SharedKernel;

/// <summary>Delivery channels supported across the platform.</summary>
public enum Channel
{
    Email = 1,
    Sms = 2,
    Push = 3,
    WhatsApp = 4,
}
